DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
SESSION_SECRET=change-me-to-a-long-random-string
PLAY_JWT_SECRET=change-me-shared-with-gateway
NEXT_PUBLIC_GATEWAY_URL=http://alarm-gw.local:8787
# Comma-separated action ids the UI can request (must match gateway ACTIONS)
NEXT_PUBLIC_BELL_ACTIONS=bells.period_start:Period start,bells.period_end:Period end,bells.chapel:Chapel
NEXT_PUBLIC_EVACUATE_ACTION=evacuate.main:Building evacuation
